# 🎯 הפרומפט האולטימטיבי להקמה אוטונומית ב-100% ע"י AI (ChatGPT / Claude / Antigravity)

מדריך זה נועד לפתור אחת ולתמיד את תופעת ה"פינג-פונג" וההתפלספויות של סוכני בינה מלאכותית (כגון ChatGPT), ומבטיח שה-AI יבצע את כל העבודה מקצה לקצה בהנחיה אחת בודדת, ללא שאלות ביניים וללא עיכובים.

---

## 📋 הוראות שימוש:
1. פתחו שיחה חדשה ב-ChatGPT (או כל סוכן AI אחר).
2. העלו לו את קובץ ה-ZIP של הפרויקט: `ai_phone_line_gemini_audio_v2_upgraded.zip`.
3. העתיקו את התבנית שלהלן, מלאו בתוכה את 4 הפרטים שלכם, ושלחו לו בהודעה אחת:

---

```text
======================================================================
הוראת ביצוע אוטונומית להקמת קו טלפון אישי עם בינה מלאכותית (גרסה 2.0)
======================================================================

העליתי לך את קובץ ה-ZIP של הפרויקט: ai_phone_line_gemini_audio_v2_upgraded.zip
מערכת זו כבר נבדקה, קומפלה, ותוקנה בייצור מלא (Node.js + @google/generative-ai + ימות המשיח).

----------------------------------------------------------------------
פרטי המערכת והמפתחות שלי:
----------------------------------------------------------------------
1. מפתח Google Gemini API:
   [הדבק כאן את מפתח ה-Gemini שלך]

2. טוקן API של ימות המשיח:
   [הדבק כאן את הטוקן שמתחיל ב-WU1BUElL...]

3. מספר המערכת בימות המשיח:
   [הדבק כאן את מספר המערכת, למשל 0772267036]

4. קישור למאגר GitHub שלי:
   [הדבק כאן קישור למאגר פתוח או קיים שלך, למשל https://github.com/USER/ai-phone-line.git]

5. סיסמה רצויה לממשק הבקרה:
   1234

----------------------------------------------------------------------
הנחיות ביצוע מחייבות ללא פשרות (NON-NEGOTIABLE DIRECTIVES):
----------------------------------------------------------------------
1. אפס שאלות ופינג-פונג: כל הפרטים הנדרשים נמסרו לעיל. אל תשאל שאלות הבהרה, אל תבקש אישורים ביניים, ורוץ ישירות עד סיום כל הפעולות.
2. אפס שכתובים ואפס ביקורת קוד (No Code Review): הקוד בקובץ ה-ZIP כבר נבדק ואושר ב-100% פעילות. אין לשנות את ה-SDK, אין לכתוב קבצי מעקף (כגון bootstrap.js), אין לשנות שמות מודלים, ויש להריץ ישירות node server.js.
3. סדר הפעולות האוטונומי שעליך לבצע:
   א. חלץ את קובץ ה-ZIP והעלה את כל הקבצים למאגר ה-GitHub שצוין לעיל.
   ב. פרוס את המאגר כ-Web Service חדש ב-Render (Runtime: Node, Plan: Free, Build: npm install, Start: npm start).
   ג. הגדר ב-Render את משתני הסביבה המדויקים הבאים:
      - GEMINI_API_KEYS = [מפתח ה-Gemini שנמסר לעיל]
      - YEMOT_API_KEY = [טוקן ימות המשיח שנמסר לעיל]
      - DASHBOARD_PASSWORD = 1234
      - GEMINI_MODELS = gemini-3-flash-preview,gemini-3.8-flash,gemini-3.6-flash,gemini-3.5-flash,gemini-3.5-flash-lite
      - PER_MODEL_TIMEOUT_MS = 20000
      - REQUEST_TIMEOUT_MS = 55000
      - MODEL_COOLDOWN_MS = 3600000
   ד. לאחר קבלת ה-URL של השירות ב-Render (לדוגמה https://xxx.onrender.com), הגדר ב-Render גם את:
      - PUBLIC_BASE_URL = https://xxx.onrender.com
   ה. הגדרת ימות המשיח והעלאת קובצי השמע:
      הרץ את סקריפט האוטומציה המובנה בתיקייה:
      node yemot_setup/auto_setup_yemot.js [טוקן ימות המשיח] [כתובת השרת ברנדר] 1
      (פעולה זו תגדיר אוטומטית את שלוחה 1 ותעלה את קובץ המוזיקה M0000.wav ואת קובץ השקט M1000.wav שמבטל את הצורך להקיש פעמיים 1).
   ו. בדיקת אימות: שלח בקשת GET לכתובת https://xxx.onrender.com/health וודא סטטוס online.

----------------------------------------------------------------------
פלט סיום נדרש:
----------------------------------------------------------------------
בסיום, הצג אך ורק כרטיס סיכום קצר בן 4 שורות:
- כתובת השרת ב-Render
- סטטוס השלוחה בימות המשיח
- קישור ללוח הבקרה והסיסמה
- אישור שהקו מוכן לחיוג מיידי!
======================================================================
```
